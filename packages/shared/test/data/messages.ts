import { DialogsMock } from './dialogs';
import { UsersMockData } from './users';

const FirstMessage = { id: '1', text: 'Test message', dialog: DialogsMock[0], user: UsersMockData[0] };

export const MessagesMock = [FirstMessage];
